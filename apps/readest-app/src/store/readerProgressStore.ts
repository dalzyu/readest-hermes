import { BookProgress } from '@/types/book';

export function useBookProgress(_bookHash: string): BookProgress | null {
  return null;
}

export function getBookProgress(_bookHash: string): BookProgress | null {
  return null;
}
